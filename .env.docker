# 🐳 Configuration Docker pour Développement
# Clean Architecture NestJS

# 🆔 IDs Utilisateur/Groupe pour éviter problèmes permissions
# Ces valeurs seront automatiquement configurées par les scripts
USER_ID=1000
GROUP_ID=1000

# 🌍 Variables d'environnement de développement
NODE_ENV=development
PORT=3000
DEBUG=*

# 🗄️ Base de données PostgreSQL (Development)
DB_TYPE=postgres
DB_HOST=postgres
DB_PORT=5432
DB_NAME=cleanarchi_dev
DB_USERNAME=dev_user
DB_PASSWORD=dev_password123

# 🔐 JWT Configuration (Development - Non-secure keys)
ACCESS_TOKEN_SECRET=development-access-secret-not-for-production
REFRESH_TOKEN_SECRET=development-refresh-secret-not-for-production
ACCESS_TOKEN_EXPIRATION=900
REFRESH_TOKEN_EXPIRATION_DAYS=7

# 🔒 Bcrypt Configuration
BCRYPT_ROUNDS=10

# 📊 Logging Level
LOG_LEVEL=debug
